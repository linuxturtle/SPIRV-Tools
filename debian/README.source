The process for updating this package to a new upstream commit/release usually
goes something like this:

# git checkout upstream-unstable
# git reset --hard <new upstream commit ID>
# git checkout -b d-u
# git merge -s ours debian-unstable
# git diff --binary debian-unstable -- debian .gitignore | git apply -R --index
# debian/get-external.sh
# git add external
# dch
# git commit ...
# debian/rules gentarball
